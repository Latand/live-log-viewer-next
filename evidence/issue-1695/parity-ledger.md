# Kanban desktop board (#1695): open parity items

The implementation plan on #1695 carries the full parity ledger, capability by capability. This file keeps
the items that a merged or open slice left unfinished, so none is lost between slices. An item leaves this
list only when the change that closes it lands on `main`.

| Item | Where it shows | Why it is open | Closes in |
| --- | --- | --- | --- |
| Receipt cap drops Retry receipts | A bulk Undo refused for more than two tasks at once: the board shows at most three receipts, so the oldest error receipt and its Retry scroll away | Found after K4b review; the groups stay hidden and are listed in the Hidden tray | A later board slice |
| Conflict after a blur-save takes focus | A same-field conflict found after the operator left the field reopens the editor, which takes focus from where they went | Recorded with the K4b follow-ups | A later board slice |
| Escape on an edit notice | Escape on a focused Use theirs or Keep mine button does not cancel the edit; Escape works from the field | Recorded with the K4b follow-ups | A later board slice |
| Retry, skip and stage edits from outside the board state no expectation | The scheme board's pipeline hub, strip, editor and verdict popover and the phone board's pipeline screen send `retry-stage` and `skip-stage` without `expectedStageId` or `expectedAttempt`, and the scheme board's stage placeholder sends `override-stage` without `expectedStageDigest`. The MCP `pipeline_action` schema describes no action-specific fields, the guards included. | The guards are optional so every existing caller keeps its behaviour; K5c changed only the kanban board's callers | A later slice for each surface |
| An undelivered edit cannot be sent to the started conversation | A first message edited while its stage started stays beside the stage, marked not delivered, with Copy text, Open conversation and Discard | No route puts text into a conversation's composer from outside it, and sending it at once would speak for the operator without their say | A later board slice |
| Role, engine and model of a waiting stage | The prototype's waiting stage has "Change role or engine…" in its menu and a model pill in its closed composer | The board's waiting stage shows its engine and model, and its account chip and "Run on account…" (K6a). The scheme board's stage placeholder changes the rest today. | A later slice |
| Cancel or change a pending account switch | The prototype's picker offers Cancel switch and a change of target while a conversation's switch waits for its turn | Implemented in K6b (#1705): the picker's Cancel switch, and Change as a cancel followed by the new switch once the cancel is confirmed, over `cancel {expectedRevision}` for a switch whose record is `requested` or `waiting-turn` and `withdraw {operationId}` for one still queued. The messages that switch held go back to the conversation's account with their payload and identity, and the migration tick and the reaper leave the ones sent before it alone; a cancelled or withdrawn reconfigure never applies, and the queue settles it once. A held record from before fences existed whose owner cannot be proven stays held, untouched. | K6b, when it merges |
| Messages across a completed account switch | Plan §6.8 and the picker: a message sent while a switch waits for its turn is delivered after it | Implemented in K6c (#1709): the committing switch carries each message it provably owns and that was never attempted to the successor, with its payload, identity and admission order, and the ordinary migration tick delivers it once. A later send cannot overtake it: claims wait for earlier admissions, and every actuator claims and reaches the journal inside one per-conversation section. Messages bound to the previous turn, injected context, client-held attachments and attempted messages end failed with their payload kept and a receipt that says whether a resend is safe. | K6c, when it merges |
| A reseated stage conversation settles on its own verdict | Plan §6.8: a stage conversation switched to another account settles on its own verdict, with no `onFail` activation | K6b's engine regression, plus K6c's integration: on an isolated registry, a stage's conversation switches through the structured reconfigure, the message held for the switch is carried and delivered by the ordinary tick, and the stage passes on the successor's verdict with no fail edge | K6c, when it merges |
| Deliveries a completed switch cannot attribute | Release limitation of K6c: legacy held records without ownership proof, a fence naming another operation, older-generation assignments, missing or settled owners | They stay exactly as they are: not delivered, never failed or deleted, still counted toward the conversation's pending limit and keeping a rolled-back block from clearing; new sends are not held behind them. Recovery is an explicit send. See `k6c-plan.md`, "Release limitations". | Open |
| Conversation actions in a pane's ⋯ | The prototype's pane ⋯ lists Interrupt the turn and Open as a full pane beside the stage actions | On the board a pane's ⋯ holds the stage actions. The conversation's own ⋯ (full pane, link, Link/Unlink, Stop host) sits at the end of its identity row, and Interrupt is in its control strip. | Open decision |
| Stage names | Graph nodes and chips name a stage by its role (Builder, Reviewer…), or by its id when two stages share a role. The prototype's fixture gives stages their own labels (Implement, Review…). | Production stages carry no label field. The naming follows K2. | Open decision |

## Closed on main

| Item | What closed it |
| --- | --- |
| Stages button and pipeline actions on the card | K5b (#1703, merged as 332b7931): Stages, the pipeline menu, the Stages sheet and waiting stages' first messages |
| C7 stage digest guard | K5c (#1704, merged as 098932f8): `override-stage` checks `expectedStageDigest` inside the mutation that writes and answers 409 `STAGE_CHANGED`; `GET /api/pipelines/:id` answers `stageDigests`; the board's save sends the digest it read |
| Server guard for retry and skip | K5c (#1704, merged as 098932f8): `retry-stage` and `skip-stage` check `expectedStageId` and `expectedAttempt` (`0` for no own attempt) before any effect; the board's pipeline menu sends the stage and attempt it showed |

## Accepted corrections that supersede the prototype

Where the operator accepted a correction to the approved prototype, the correction is the requirement, and
the board follows it even where the prototype's own code does something else.

| Correction | Prototype behaviour it supersedes | What the board does |
| --- | --- | --- |
| #1695 binding correction 4, accepted 2026-09-14: "compact pipeline summary in cards, detailed graph in Stages" | `renderPipeline` opens the stage graph by default on an active workspace card with up to four stages | Every card starts on the compact summary. The toggle opens the graph and is remembered per card while the board is open. The detailed graph's full home is the Stages sheet (K5b). |

## Product rules the prototype had no case for

| Rule | Why | What the board does |
| --- | --- | --- |
| Retry and skip act on the stage the pipeline waits on | The engine's `retry-stage` and `skip-stage` apply to the cursor stage of a `needs_decision` pipeline; a stage id on `retry-stage` asks for a launch-receipt retry instead. | The pipeline menu names that stage ("Retry Verifier") and sends the action with the stage and attempt it showed (`expectedStageId`, `expectedAttempt`). The engine refuses when another stage or a newer attempt waits now, and the receipt names what waits. Elsewhere, and in the pane of any other stage, both are disabled with the engine's reason. The prototype's "Retry a stage…" chooser has no engine action behind it. |
| A write with no answer is not a refusal | A request that failed on its way back, or an answer without the route's error, may or may not have run. | Pipeline actions say "not confirmed" with a Check again that only reads and reports what the pipeline shows; nothing is resent. A stage message save says "Not confirmed", keeps the text, and settles only on what the stage holds. |
| Which accounts the picker offers | The server decides. A waiting stage's first turn may use only an account the project's binding allows (`override-stage` refuses the rest), and a running conversation may switch to any signed-in account, one outside the project's accounts recorded as the operator's choice (#1279). Neither route refuses an account at its usage limit. | A waiting stage's picker marks accounts the binding refuses as not allowed and unavailable; a conversation's picker marks them outside this project's accounts and keeps them available. An account at its limit is tagged with its reset and stays available on both, where the prototype disabled it. Signed-out accounts are unavailable for a conversation, as the conversation header's chip has them. |
| Lineage-adopted (`historical`) attempts are evidence, not stage attempts | The engine appends them when a stage agent brings in a conversation. It copies the source attempt's provenance onto them and excludes them from the fail budget. `latestOperationalStageAttempt` is the canonical selector. | Graph counts, fail budgets, the current attempt, node selection and live-edge marks all ignore them. Their conversations stay reachable in Past attempts, under "Helper conversations", labelled as helpers. |
